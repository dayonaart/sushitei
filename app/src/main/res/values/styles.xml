<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.ButtonActionBar" parent="Widget.AppCompat.ActionButton">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <style name="AppTheme.Toolbar">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:minHeight">?attr/actionBarSize</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/AppTheme.TabLayout.TextApparence</item>
        <item name="tabGravity">center</item>
    </style>

    <style name="AppTheme.TabLayout.TextApparence" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textSize">10sp</item>
        <item name="fontPath">fonts/latoLight.ttf</item>
    </style>

    <style name="AppTheme.TextLight">
        <item name="textAllCaps">false</item>
        <item name="android:textSize">@dimen/text_size_title_material</item>
        <item name="fontPath">fonts/latoRegular.ttf</item>
    </style>

    <style name="AppTheme.TextTransparent">
        <item name="textAllCaps">false</item>
        <item name="android:textColor">@android:color/transparent</item>
        <item name="android:textSize">0dp</item>
        <item name="fontPath">fonts/latoRegular.ttf</item>
    </style>

    <style name="TextAppearanceAccent">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="TextAppearanceBlack">
        <item name="android:textColor">@color/colorBlack</item>
    </style>

</resources>
